import { Inter, JetBrains_Mono } from "next/font/google";

export const primary = Inter({
  weight: ['700', '400'],
  variable: '--font-primary',
  subsets: ['latin'],
})

export const secondary = JetBrains_Mono({
  weight: ['700', '400'],
  variable: '--font-secondary',
  subsets: ['latin'],
})
